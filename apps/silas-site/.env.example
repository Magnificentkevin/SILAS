# Where the "Staff Login" CTA points — the SILAS Command Portal (apps/web-client),
# meant to live at a subdomain now that this app owns the root silaserv.com domain.
NEXT_PUBLIC_PORTAL_URL=https://portal.silaserv.com
