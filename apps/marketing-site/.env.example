# Resend API key — create one at https://resend.com/api-keys
# The sending domain (allbusinesscleaning.com) must be verified in Resend first
# (Resend dashboard -> Domains -> Add Domain -> add the DNS records it gives you
# at your DNS host).
RESEND_API_KEY=

NEXT_PUBLIC_SITE_URL=https://allbusinesscleaning.com
